# Dependencies are declared in pyproject.toml. This file is a convenience
# installer that mirrors them. Prefer:  pip install -e ".[all]"
#
# Runtime (core):
pandas>=2.0
numpy>=1.24
#
# Optional extras (uncomment as needed, or use pip install -e ".[all]"):
# yfinance>=0.2.40      # live Yahoo Finance data
# pyarrow>=14.0         # Parquet caching
# matplotlib>=3.7       # equity-curve / Monte-Carlo charts
# aiohttp>=3.9          # Tradovate live/sandbox execution
# python-dotenv>=1.0    # .env loading
